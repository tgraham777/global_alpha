<div class="container" id="country-container">
  <h1><%= @country.name %></h1>
  <h3>Tags</h3>
  <ul>
    <% @country.tags.each do |tag| %>
      <li><%= tag.name %></li>
    <% end %>
  </ul><br>
</div>
