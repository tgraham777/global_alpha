<div class="container" id="country-index-container">
  <h1>Country Index</h1>

  <ul>
    <% @countries.each do |country| %>
      <li><%= link_to country.name.split.map(&:capitalize).join(' '), country_path(country) %></li>
    <% end %>
  </ul>
</div>
