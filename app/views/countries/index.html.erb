<div class="container" id="country-index-container">
  <h1><span style="font-size: 25px;" class="glyphicon glyphicon-globe"></span> Countries</h1>

  <ul>
    <% @countries.each do |country| %>
      <li><%= link_to country.name, country_path(country) %></li>
    <% end %>
  </ul>
</div>
