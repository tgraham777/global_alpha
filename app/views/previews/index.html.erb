<div class="container" id="preview-index-container">
  <h1>Previews</h1>

  <% @previews.each do |preview| %>
    <hr><h2><%= link_to preview.title, preview_path(preview) %></h2>
    <h4><em><%= preview.report_date %></em></h4>
    <%= truncate(preview.intro, length: 250) %>
  <% end %>
</div>
